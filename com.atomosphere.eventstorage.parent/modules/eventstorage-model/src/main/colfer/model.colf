package colfer

type Event struct {
	key						binary
	value					binary
	lastValue				binary
	timestamp				uint64
	version					uint32
}

type EventVersion struct {
	key						binary
	version					uint32
}
